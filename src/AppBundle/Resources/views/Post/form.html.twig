{{ form_start(
    form,
    {'attr': {'data-autosave-url': path('post_autosave', {'id': post.id})}}
) }}
    {{ form_widget(form) }}
<input
        type="submit"
        value="Save"
        class="btn btn-default"
>
{{ form_end(form) }}
