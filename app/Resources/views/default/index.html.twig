{% extends 'FOSUserBundle::layout.html.twig' %}

{% block title %}
    Home {{ parent() }}
{% endblock %}

{% block body %}
    <ul>
        {% for post in posts %}
            <li>
                {{ post.title }}
            </li>
        {% endfor %}
    </ul>
{% endblock %}
