build:
    environment:
        selenium: true
    project_setup:
        before:
            -
                command: bin/console server:start
    tests:
        override:
            -
                command: codecept run

checks:
    php:
        code_rating: true
        duplication: true
